## Production-ready automotive profile + vehicle sales website with stable storage, live CMS admin, media manager, and interactions

To run app build and run docker image: `docker build -t app . docker run -it --network host app`
